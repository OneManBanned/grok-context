# grok-context
grok-context is a lightweight Bash script that recursively scans a directory, outputting relative file paths and their contents to a context.txt file. You can then use this file as context for llm that don't expect files as context (grok3).
